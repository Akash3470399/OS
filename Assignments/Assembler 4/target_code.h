#include <stdlib.h>
#include <stdio.h>

#include "utilityFunc.h"

#ifndef TARGET_CODE_H
int createTargetCode(char *ic_line)
{
    // char *tokens[5], *code = (char *)malloc(sizeof(20));
    // int tokenCount = tokenizer(ic_line, tokens)
    char sclass[3], op1[10], op2[10];
    int mn;
    // sscanf(ic_line, "<%s, %d", sclass, mn);

    // printf("t: %s %s\n", sclass, op1);
}

#endif